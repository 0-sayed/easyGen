# Build Version Label Clarity

Change the API status detail label from `Version` to `Build version`.

Update the focused frontend test that verifies the label. Do not change the
version value, API response, or surrounding layout.
